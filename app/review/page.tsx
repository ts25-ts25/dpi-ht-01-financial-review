import Link from "next/link";
import submission from "@/data/submission.json";
const euro=new Intl.NumberFormat("en-IE",{style:"currency",currency:"EUR",maximumFractionDigits:0});
const amount=(n:number|null)=>n===null?"—":n<0?`(${euro.format(Math.abs(n))})`:euro.format(n);
export default function Review(){const material=submission.decisions.filter(d=>d.reviewTier==="material_judgment");const low=submission.decisions.filter(d=>d.confidence==="low");const disagreements=material.filter(d=>/prefers|but/i.test(d.independentChallenge));return <main className="review"><header className="site-header"><Link className="brand" href="/">DPI <i>HT 01</i></Link><nav><Link href="/">Main view</Link><a href="/submission.json">Submission JSON</a></nav></header><section className="review-head"><p className="eyebrow">Assessor view</p><h1>Certification review</h1><p className="lede">A compact trail of material judgments, independent analysis, financial effects, and items requiring student approval.</p></section><section className="review-metrics">{[["Material judgments",material.length],["Independent differences",disagreements.length],["Low-confidence items",low.length],["Student certification","Pending"]].map(([l,n])=><div key={l as string}><span>{l}</span><b>{n}</b></div>)}</section><section className="review-section"><h2>Material judgment trail</h2><div className="trail">{material.map(d=><article className="trail-card" key={d.id}><div className="trail-head"><span className="id">{d.id}</span><span className={`pill ${d.confidence}`}>{d.confidence} confidence</span></div><h3>{d.question}</h3><div className="trail-copy"><p><strong>AI proposal</strong>{String(d.aiProposal)}</p><p><strong>Independent challenge</strong>{d.independentChallenge}</p><p><strong>Student certification</strong>{d.studentReasoning}</p></div><div className="effect"><span>Profit {amount(d.statementEffect.profit)}</span><span>Assets {amount(d.statementEffect.assets)}</span><span>Liabilities {amount(d.statementEffect.liabilities)}</span><span>Equity {amount(d.statementEffect.equity)}</span></div><small>Evidence: {d.evidence.join(" · ")} · Student override: {d.changedFromAI?"Yes":"No recorded"}</small></article>)}</div></section><section className="review-section split"><div><h2>Low-confidence items</h2>{low.map(d=><article className="flag-card" key={d.id}><span className="id">{d.id}</span><h3>{d.question}</h3><p>{String(d.answer)}</p><small>Evidence: {d.evidence.join(" · ")}</small></article>)}</div><div><h2>Unresolved uncertainty</h2>{submission.uncertainties.map(u=><article className="flag-card" key={u.matter}><h3>{u.matter}</h3><p>{u.effect}</p></article>)}</div></section></main>}
